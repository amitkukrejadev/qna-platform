# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local        # 🆕 specific local env overrides
.env.development  # 🆕 for local-only dev values
.env.production   # 🆕 ignore unless version-controlled on purpose

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE / system
.vscode/          # 🆕 VSCode settings
.idea/            # 🆕 JetBrains IDEs (WebStorm, etc.)
*.swp             # 🆕 Vim swap files
*.swo             # 🆕 Vim temp files
*.tmp             # 🆕 general temp files

# system logs or macOS junk
Thumbs.db         # 🆕 Windows thumbnail cache
ehthumbs.db       # 🆕 More Windows cache
Icon?             # 🆕 macOS icon
Desktop.ini       # 🆕

# user-specific config or secret files
src/models/client/config.ts  # 🆕 optional if sensitive or purely local

